pre-commit:
  parallel: true
  commands:
    rubocop:
      run: bundle exec rubocop
    reek:
      run: bundle exec reek .
    rails_best_practices:
      run: bundle exec rails_best_practices
    # test:
    #   run: rspec spec
